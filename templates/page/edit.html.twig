{% extends 'adminbase.html.twig' %}

{% block title %}Edition de la page : {{page.title}}{% endblock %}

{% block body %}
    <div class="row">
        <div class = "col-8">
           <h1>Edition de la page : {{ page.title}}</h1>
        </div>
        <div class = "col-4">
            <div>
                <a href="{{ path('page_index') }}">retour à la liste des pages</a>
                {{ include('page/_delete_form.html.twig') }}
                {{ form_start(form) }}
    <button class="btn">{{ button_label|default('Enregistrer') }}</button>
            </div>
        </div>
    </div>
<div class="form-check form-check-inline">
    {{ form_row(form.published, {label_attr: {class: 'checkbox-custom'} }) }}
</div>
{{ form_row(form.title)}}
{{ form_row(form.parent)}}
<fieldset>
<legend> Mise en avant</legend>
    <div class="form-check form-check-inline">
        {{ form_row(form.topPage, {label_attr: {class: 'checkbox-custom'} }) }}
    </div>
    {{form_widget(form.topImage.image) }}
    {{form_row(form.resume)}}
</fieldset>
<h2>Ajouter des images</h2>
	    {{ include('page/_uploadImage.html.twig', {'files': page.images, 'pageId': page.id}) }}
    {{ form_widget(form) }}
{{ form_end(form) }}


{% endblock %}
